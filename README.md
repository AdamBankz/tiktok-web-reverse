# TikTok Web Reverse

`mssdk-info` is a security header generated by TikTok’s MSSDK. It’s essentially an encoded blob of fingerprint data.

`verifyFp` is a value generated by TikTok’s web JavaScript. It’s essentially a time-based, UUID-style fingerprint.

`strData` is essentially a compressed, encrypted, and obfuscated bundle of browser fingerprint data.

#
[Click here](https://tiktool.mysellauth.com/) for premium TikTok tools (viewbot, followbot & more)<br>
**Contact @adambankz on Telegram for full reverse (x-B, x-G & more)**
